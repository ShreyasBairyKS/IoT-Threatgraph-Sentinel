# IoT ThreatGraph Sentinel - Backend (P3)
# Pin versions for reproducibility

fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
pydantic-settings==2.7.0
websockets==14.1

# Day 3 additions (uncomment when needed)
# reportlab==4.2.5       # PDF generation
# httpx==0.28.1          # Async HTTP for P1/P2 service calls

# Dev / testing
pytest==8.3.4
pytest-asyncio==0.25.0
httpx>=0.27.0,<0.28.0   # needed for TestClient with async FastAPI; upper-bounded for system compat
